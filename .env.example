# Supabase transaction pooler (port 6543) for application queries.
DATABASE_URL=""
# Supabase direct/session pooler (port 5432) for Prisma migrations.
DIRECT_URL=""
NEXT_PUBLIC_SUPABASE_URL=""
# Use the publishable key; legacy projects may use the anon key name below.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
# Optional and server-only. The current app does not require this key.
SUPABASE_SERVICE_ROLE_KEY=""
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# AI is optional. Without both values, onboarding creates a clearly labelled starter plan.
OPENAI_API_KEY=""
OPENAI_TEXT_MODEL=""
